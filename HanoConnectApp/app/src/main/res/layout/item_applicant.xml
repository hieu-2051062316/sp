<?xml version="1.0" encoding="utf-8"?>
<!-- Layout cho một hàng (item) trong danh sách ứng viên -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:padding="16dp"
    android:background="?attr/selectableItemBackground">

    <ImageView
        android:id="@+id/ivApplicantAvatar"
        android:layout_width="48dp"
        android:layout_height="48dp"
        tools:src="@drawable/ic_person_placeholder" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:layout_marginStart="16dp">

        <TextView
            android:id="@+id/tvApplicantName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@android:color/black"
            android:textSize="16sp"
            android:textStyle="bold"
            tools:text="Nguyễn Văn A" />

        <TextView
            android:id="@+id/tvApplyDate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textColor="#757575"
            android:textSize="14sp"
            tools:text="Nộp ngày: 23/06/2025" />
    </LinearLayout>

    <!-- Thay thế nút "Xem CV" bằng TextView hiển thị trạng thái -->
    <TextView
        android:id="@+id/tvApplicantStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:textStyle="bold"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingTop="4dp"
        android:paddingBottom="4dp"
        android:background="@drawable/edit_text_background"
        tools:text="Pending"
        tools:textColor="@color/status_pending" />

</LinearLayout>
